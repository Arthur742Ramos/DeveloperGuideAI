# Sample Configuration File for Secret Redaction Practice
# This is SYNTHETIC data - demonstrates secrets that should never go to AI

# Database credentials - REDACT BEFORE SHARING
DATABASE_URL=postgresql://admin:SuperSecret123!@db.example.com:5432/production
DATABASE_PASSWORD=SuperSecret123!
DATABASE_USER=admin

# API Keys - REDACT BEFORE SHARING
STRIPE_SECRET_KEY=sk_test_EXAMPLE_REPLACE_WITH_YOUR_KEY
STRIPE_PUBLISHABLE_KEY=pk_test_EXAMPLE_REPLACE_WITH_YOUR_KEY
AWS_ACCESS_KEY_ID=AKIAEXAMPLE_REPLACE_ME
AWS_SECRET_ACCESS_KEY=EXAMPLE_SECRET_KEY_REPLACE_WITH_YOUR_OWN
OPENAI_API_KEY=sk-EXAMPLE-REPLACE-WITH-YOUR-OWN-KEY
GITHUB_TOKEN=ghp_EXAMPLE_REPLACE_WITH_YOUR_TOKEN

# OAuth secrets - REDACT BEFORE SHARING
GOOGLE_CLIENT_SECRET=GOCSPX-xxxxxxxxxxxxxxxxxxxxx
AUTH0_CLIENT_SECRET=xxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# JWT secrets - REDACT BEFORE SHARING
JWT_SECRET=my-super-secret-jwt-signing-key-do-not-share
SESSION_SECRET=another-secret-for-sessions

# Safe to share (non-sensitive configuration)
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
CACHE_TTL=3600
MAX_UPLOAD_SIZE=10485760
FEATURE_FLAG_NEW_UI=true

# External service URLs (safe if not containing credentials)
API_BASE_URL=https://api.example.com/v1
CDN_URL=https://cdn.example.com
REDIS_HOST=redis.example.com
REDIS_PORT=6379

# Exercise Instructions:
# Before sharing code/config with AI for debugging:
#
# 1. Replace actual secrets with placeholders:
#    DATABASE_PASSWORD=<REDACTED>
#    STRIPE_SECRET_KEY=sk_live_<REDACTED>
#
# 2. Use fake but realistic-looking values for context:
#    AWS_ACCESS_KEY_ID=AKIAEXAMPLE
#    AWS_SECRET_ACCESS_KEY=<YOUR_SECRET_KEY>
#
# 3. Keep non-sensitive config to help AI understand context:
#    PORT, LOG_LEVEL, feature flags, etc.
#
# 4. Sanitize connection strings:
#    Before: postgresql://admin:password@host/db
#    After:  postgresql://user:****@host/db
